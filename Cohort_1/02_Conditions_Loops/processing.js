// processesing is condition and loops

// server call, rainy, cloudy, sunny

let weather = "cloudy";
// = means assignment
// == means comparison
// === means strict comparison

let result = weather === "rainy";
// === strictly equal which means data type should be same

if (weather === "rainy") {
  console.log("it's raining, umbrella le jaana bhai");
} else if (weather === "cloudy") {
  console.log("jacket le ja bhai");
} else if (weather === "sunny") {
  console.log("sunny h ji aaj to");
}

console.log(weather == "rainy"); // returns boolean
